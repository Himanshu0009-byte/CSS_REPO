# CSS_REPO
## Styling Custom CSS is used to style the site's semantic structure, including a  responsive grid layout for media items, consistent typography and color  scheme, styled `&lt;audio>`/`&lt;video>` players, and media queries for mobile  responsiveness.
